version: 2
jobs:
  build:
    docker:
      - image: aparker/circleci-dotnet-mono:latest
    steps:
      - checkout
      - run: msbuild Zilon.Core/Zilon.Game.sln /t:Restore
      - run: msbuild Zilon.Core/Zilon.Game.sln /t:Build